{
  "name": "@elasticpath/mason",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "generate": "turbo run generate --parallel",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "examples": "turbo run build --filter=@elasticpath/mason-cli... && yarn run scaffold:local:simple",
    "scaffold:local": "yarn scaffold:setup && concurrently \"yarn:scaffold:local:*\"",
    "scaffold:setup": "mkdir -p examples && cd ./examples && rimraf ./*",
    "scaffold:local:simple": "cd ./examples && node ../packages/mason-cli/dist/bin/mason.js ../packages/d2c-schematics/dist:d2c --dry-run=false --skip-install=true --skip-git=true --interactive=false --epcc-client-id=abc123 --epcc-client-secret=def456 --epcc-endpoint-url=api.moltin.com basic",
    "scaffold:local:algolia": "cd ./examples && node ../packages/mason-cli/dist/bin/mason.js ../packages/d2c-schematics/dist:d2c --dry-run=false --skip-install=true --skip-git=true --epcc-client-id=abc123 --epcc-client-secret=def456 --epcc-endpoint-url=api.moltin.com --plp-type=Algolia algolia"
  },
  "devDependencies": {
    "@changesets/cli": "^2.24.1",
    "@playwright/test": "^1.28.1",
    "concurrently": "^7.6.0",
    "dotenv-cli": "^7.1.0",
    "eslint-config-custom": "*",
    "mkdirp": "^2.1.5",
    "prettier": "latest",
    "quicktype-core": "^6.1.0",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.18"
}
