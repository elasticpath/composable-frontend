---
"@epcc-sdk/sdks-accounts": minor
---

Add zod schemas at `@epcc-sdk/sdks-accounts/zod`, generated from the account management spec. `zod@^3.22.0` is an optional peer dependency: install it only if you import `/zod`. The root entry does not import zod.
