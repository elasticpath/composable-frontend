// This file is auto-generated by @hey-api/openapi-ts

export type RulePromotionItem = RulePromotionBaseAttributes &
  RulePromotionBaseRuleSetBaseAttributes

export type RulePromotionResponse = {
  data?: RulePromotionItem
}

export type RulePromotionListResponse = {
  data?: Array<RulePromotionItem>
  links?: PaginationLinks
  meta?: PaginationMeta
}

/**
 * Represents a single rule promotion usage record.
 */
export type RulePromotionUsage = {
  /**
   * The unique identifier of the usage record.
   */
  id?: string
  /**
   * The associated order ID, if applicable.
   */
  order_id?: string | null
  /**
   * The unique identifier of the promotion code.
   */
  code_id?: string
  /**
   * The promotion code used.
   */
  code?: string
  /**
   * The number of times the promotion code has been used.
   */
  times_used?: number
  /**
   * The timestamp when the promotion was applied.
   */
  used_on?: Date
  /**
   * The customer identifier who used the promotion, if applicable.
   */
  customer_id?: string | null
  /**
   * The account identifier associated with the promotion usage, if applicable.
   */
  account_id?: string | null
  /**
   * The email of the customer who used the promotion, if applicable.
   */
  customer_email?: string | null
  meta?: {
    timestamps?: {
      /**
       * The timestamp when the usage record was last updated.
       */
      updated_at?: Date
    }
  }
  /**
   * Identifier of the user or system that last updated the record.
   */
  updated_by?: string
  /**
   * Indicates whether the usage record has been anonymized.
   */
  anonymized?: boolean
}

export type RulePromotionBaseAttributes = {
  type?: "rule_promotion"
  /**
   * Specifies a name for the promotion.
   */
  name?: string
  /**
   * Specifies a description for the rule promotion.
   */
  description?: string
  /**
   * Specifies the priority order of a promotion, with larger numbers indicating higher priorities.
   */
  priority?: number
  /**
   * Specifies whether the promotion is enabled. The options are true or false, and the default setting is false.
   */
  enabled?: boolean
  /**
   * Specifies whether the promotion is applied automatically to the cart or a code is required to apply the promotion. The default setting is false. When this value is set to true, a code is autogenerated. If this value is set to false, you must create the code manually.
   */
  automatic?: boolean
  /**
   * Specifies whether this promotion can stack with other promotions.
   */
  stackable?: boolean
  /**
   * Enables a promotion to be stacked with an otherwise non-stackable promotion.
   */
  override_stacking?: boolean
  /**
   * Specifies the start date and time of the promotion or the start date of the promotion. You can provide a specific time in the HH:MM format. If no time is specified, the default start and end time is set to 00:00.
   */
  start?: Date
  /**
   * Specifies the end date and time of the promotion or the end date of the promotion.
   */
  end?: Date
  /**
   * Unique identifier for the promotion.
   */
  id?: string
  /**
   * Store identifier for the promotion.
   */
  store_id?: string
  /**
   * User or system that created the promotion.
   */
  created_by?: string
  /**
   * User or system that last updated the promotion.
   */
  updated_by?: string
  meta?: {
    timestamps: {
      /**
       * Timestamp when the promotion was created.
       */
      created_at: Date
      /**
       * Timestamp when the promotion was last updated.
       */
      updated_at: Date
    }
  }
}

export type RulePromotionBaseRuleSetBaseAttributes = {
  /**
   * Controls the applicability of the promotion as well as the specific benefit or mechanic of the promotion.
   */
  rule_set?: {
    /**
     * Specifies the [catalogs](https://elasticpath.dev/docs/api/pxm/catalog/catalogs) that are eligible for the promotion. By default, the promotion applies to all items, including custom items. However, when catalog_ids is defined, the promotion is only applied to items within the specified catalogs. If catalog IDs are specified, custom items cannot be applied as part of the promotion.
     */
    catalog_ids?: Array<string>
    /**
     * Specifies [currencies](https://elasticpath.dev/docs/api/pxm/currencies/currencies) that are applied for the promotion
     */
    currencies?: Array<string>
    /**
     * The scope or applicability of the promotion (i.e., specifies what conditions must be true for a cart to be eligible for the promotion).
     */
    rules?:
      | AccountTags
      | And
      | CartCustomAttributes
      | CartItemCustomAttributes
      | CartTotal
      | ItemAttribute
      | ItemCategory
      | ItemIdentifier
      | ItemPrice
      | ItemProductId
      | ItemQuantity
      | ItemsBundle
      | ItemSku
      | Or
    /**
     * Describes how this promotion should be applied to the cart and order.
     */
    actions?: Array<
      CartDiscount | ItemDiscount | ShippingDiscount | BundleDiscount
    >
  }
}

/**
 * Specifies whether the code is consumed per application or per checkout. With `per_checkout`, the code is used once for each checkout, regardless of the number of items in the cart. When set to `per_application`, the code is used per application. For cart discounts, each application counts as one usage. For item discounts, each application to either a single quantity or a bundle is counted as one usage. For example, in a store that offers 50% off on SKU1, SKU2, and SKU3, and limits the maximum usage of the promotion code to two, a shopper can apply the promotion up to two quantities. If the cart contains two or more quantities of SKU1, the promotion is applied 2 times to SKU1, and other quantities and items are at the regular price. If the cart contains one quantity of SKU1, one quantity of SKU2, and one quantity of SKU3, the promotion is applied once to SKU1 and once to SKU2. The code usage is applied at checkout and the code is considered consumed at that point.
 *
 */
export type ConsumeUnit = "per_application" | "per_checkout"

export type PromotionCodesRequest = {
  data?: {
    type?: "promotion_codes"
    /**
     * Specifies the code details in an array of objects.
     */
    codes?: Array<{
      /**
       * Specifies the string to use as a code for the promotion.
       */
      code?: string
      /**
       * Specifies the number of times the code can be used. If no value is set, the customer can use the code any number of times.
       */
      uses?: number
      /**
       * Specifies the customer ID of the shopper who can use the code. For more information, see the [Create a customer](/docs/customer-management/customer-management-api/create-a-customer) section.
       */
      user?: string
      /**
       * Specifies whether the code is consumed per application or per checkout. With `per_checkout`, the code is used once for each checkout, regardless of the number of items in the cart. When set to `per_application`, the code is used per application. For cart discounts, each application counts as one usage. For item discounts, each application to either a single quantity or a bundle is counted as one usage. For example, in a store that offers 50% off on SKU1, SKU2, and SKU3, and limits the maximum usage of the promotion code to two, a shopper can apply the promotion up to two quantities. If the cart contains two or more quantities of SKU1, the promotion is applied 2 times to SKU1, and other quantities and items are at the regular price. If the cart contains one quantity of SKU1, one quantity of SKU2, and one quantity of SKU3, the promotion is applied once to SKU1 and once to SKU2. The code usage is applied at checkout and the code is considered consumed at that point.
       *
       */
      consume_unit?: "per_application" | "per_checkout"
      max_uses_per_shopper?: MaxUsesPerShopper
      /**
       * A flag indicating whether the coupon is for first-time shoppers. If set to `true`, the discount will only apply if the shopper has never made a payment on any order in the store. If set to `false` or left unset, it will be a regular discount that applies to all shoppers. When this flag is set to `true`, the coupon cannot have usage limitations or be assigned to specific users.
       */
      is_for_new_shopper?: boolean
    }>
  }
}

export type CreatePromotionCodesResponse = {
  data?: Array<CreatePromotionCodeResponse>
  messages?: Array<PromotionCodeMessage>
}

export type GetPromotionCodesResponse = {
  data?: Array<GetPromotionCodeResponse>
  links?: PaginationLinks
  meta?: PaginationMeta
}

export type GetPromotionCodeResponse = {
  type?: "promotion_codes"
  /**
   * Unique identifier for the promotion code
   */
  id?: string
  code?: string
  uses?: number
  user?: string
  /**
   * Specifies whether the code is consumed per application or per checkout. With `per_checkout`, the code is used once for each checkout, regardless of the number of items in the cart. When set to `per_application`, the code is used per application. For cart discounts, each application counts as one usage. For item discounts, each application to either a single quantity or a bundle is counted as one usage. For example, in a store that offers 50% off on SKU1, SKU2, and SKU3, and limits the maximum usage of the promotion code to two, a shopper can apply the promotion up to two quantities. If the cart contains two or more quantities of SKU1, the promotion is applied 2 times to SKU1, and other quantities and items are at the regular price. If the cart contains one quantity of SKU1, one quantity of SKU2, and one quantity of SKU3, the promotion is applied once to SKU1 and once to SKU2. The code usage is applied at checkout and the code is considered consumed at that point.
   *
   */
  consume_unit?: "per_application" | "per_checkout"
  max_uses?: number
  max_uses_per_shopper?: MaxUsesPerShopper
  is_for_new_shopper?: boolean
  created_by?: string
  meta?: {
    timestamps?: {
      created_at?: Date
    }
  }
}

export type CreatePromotionCodeResponse = {
  id?: string
  type?: "promotion_codes"
  code?: string
  uses?: number
  user?: string
  consume_unit?: "per_application" | "per_checkout"
  max_uses?: number
  max_uses_per_shopper?: MaxUsesPerShopper
  is_for_new_shopper?: boolean
}

export type PromotionCodeMessage = {
  /**
   * Information about the affected promotion codes.
   */
  source?: {
    /**
     * Indicates that the affected entity is a promotion code.
     */
    type?: "promotion_codes"
    /**
     * A list of promotion codes that triggered the message.
     */
    codes?: Array<string>
  }
  /**
   * A brief title summarizing the message.
   */
  title?: string
  /**
   * A detailed explanation of the message.
   */
  description?: string
}

/**
 * Successful response
 */
export type PromotionCodeExportedFileResponse = {
  /**
   * URL to download the CSV file.
   */
  href?: string
}

/**
 * Object for setting max uses per shopper. Only include this object, when you want to set limit per shopper.
 */
export type MaxUsesPerShopper = {
  /**
   * Sets max number of times the code can be used by a shopper. NOTE - This cannot be set with `per_application` consume unit.
   */
  max_uses?: number
  /**
   * The flag to include guest shoppers for the discount with max use restriction. **If this field is provided, the max_uses value is required.** When set to `true`, guest shoppers must have an email associated with the cart to use the code. A guest cart without an email cannot use the code. When set to `false`, guest shoppers cannot use the promo code, even if the cart has an associated guest email.
   */
  includes_guests?: boolean
}

/**
 * The type of job to run. For example, `code_generate` to generate codes or `code_export` to export codes.
 */
export type JobType = "code_generate" | "code_export"

export type PromotionJob = {
  /**
   * The type of job to run. For example, `code_generate` to generate codes or `code_export` to export codes.
   */
  job_type?: "code_generate" | "code_export"
  /**
   * Represents the name of the job. The maximum allowed length is 50 characters.
   */
  name?: string
  parameters?: {
    /**
     * Specifies the number of codes to be generated.
     */
    number_of_codes?: number
    /**
     * Specifies the maximum number of usages per code. If set to zero, you cannot use this promotion. If no value is set, it can be used unlimited times.
     */
    max_uses_per_code?: number
    /**
     * Specifies whether the promotion code is consumed per application or per checkout. This determines how usage limits are applied when a code is used multiple times.
     *
     */
    consume_unit?: "per_application" | "per_checkout"
    /**
     * Prefix for generated promotion codes (e.g., `SUMMER-`).
     */
    code_prefix?: string
    /**
     * Code length.
     */
    code_length?: number
  }
  /**
   * A unique ID generated when a job is created.
   */
  id?: string
  type?: "promotion_job"
  /**
   * A unique ID of a promotion.
   */
  rule_promotion_id?: string
}

/**
 * Successful response
 */
export type PromotionJobsListResponse = {
  data?: Array<PromotionJobResponse>
  links?: PaginationLinks
  meta?: PaginationMeta
}

export type ResponseError = {
  errors?: Array<{
    /**
     * HTTP status code.
     */
    status?: number
    /**
     * Error title.
     */
    title?: string
    /**
     * Error details.
     */
    detail?: string
    /**
     * Source of the error (e.g., which field or parameter caused the error)
     */
    source?: string
    meta?: {
      usage_ids?: Array<string>
    }
  }>
}

/**
 * Represents the result of a promotion job, including the number of generated and deleted promotion codes.
 *
 * - `generated`: Total number of successfully generated codes.
 * - `deleted`: Number of codes that were deleted during job cancellation, if applicable.
 *
 */
export type GenerateResult = {
  /**
   * Number of successfully generated promotion codes.
   */
  generated?: number
  /**
   * Number of codes deleted due to job cancellation.
   */
  deleted?: number | null
}

/**
 * Specifies the strategy for determining which items to discount based on their individual prices.
 */
export type PriceStrategy = "cheapest" | "expensive"

export type ItemDiscount = {
  /**
   * Applies a discount to particular items in the cart matching the supplied conditions.
   */
  strategy?: "item_discount"
  args?: FixedArg | PercentArg | FixedPriceQuantityArg
  /**
   * Specifies which items are eligible for the discount. Supports logical operations (and/or) and various item targeting strategies.
   */
  condition?: Condition
  limitations?: {
    /**
     * Specifies the maximum amount of discount applied to the shopping cart.
     */
    max_discount?: number
    /**
     * Specifies the maximum quantity of each eligible item to which the promotion is applied.
     */
    max_quantity?: number
    items?: {
      /**
       * Specifies the maximum number of items eligible for the discount.
       */
      max_items?: number
      /**
       * Specifies the maximum total quantity of units eligible for the discount across all selected items. Units are sorted by price before selection, defaulting to cheapest first. Use `price_strategy` to change the sort order. For example, with `max_units: 2` and `price_strategy: expensive`, the 2 most expensive units across all eligible items receive the discount, regardless of which SKUs they belong to.
       */
      max_units?: number
      /**
       * Specifies the strategy for determining which items to discount based on their individual prices.
       */
      price_strategy?: "cheapest" | "expensive"
      /**
       * This flag determines whether eligible items should be automatically added to the cart. This can only be set to `true` when the target item discount is 100% and the promotion is **automatic**.  Otherwise, the API returns a validation error. NOTE: Item to be auto added should be from PXM catalog.
       */
      auto_add?: boolean
      /**
       * A flag indicating whether to show suggestions for eligible items in the cart if the item is not in the cart.
       */
      show_suggestions?: boolean
    }
  }
}

export type CartDiscount = {
  /**
   * Applies a discount across the entire cart.
   */
  strategy?: "cart_discount"
  args?: FixedArg | PercentArg
  /**
   * Specifies which items are eligible for the cart-wide discount. Supports all item-based targeting strategies and logical operations.
   */
  condition?: Condition
  limitations?: {
    /**
     * Specifies the maximum amount of discount applied to the shopping cart.
     */
    max_discount?: number
  }
}

export type BundleDiscount = {
  /**
   * Applies a discount to specific combinations of items purchased together.
   */
  strategy?: "items_bundle_discount"
  args?: FixedArg | PercentArg | FixedPriceArg
  condition?: Condition
  limitations?: {
    /**
     * Specifies the maximum amount of discount applied to the shopping cart.
     */
    max_discount?: number
  }
}

export type ShippingDiscount = {
  /**
   * Applies a discount to the shipping costs on a cart
   */
  strategy?: "shipping_discount"
  args?: FixedArg | PercentArg | FixedPriceArg
  /**
   * Specifies which shipping types are eligible for the discount. Only shipping_type strategy is supported for shipping discounts.
   */
  condition?: Condition
}

/**
 * The condition that gates this action. When present, the action is applied only if the condition is satisfied.
 * **Valid top-level strategies:**
 * - `cart_total` — gates the action based on the cart total (or filtered item total).
 * Supported on `cart_discount` actions only.
 * - `and` / `or` — logical combinators. Children must all be the same level (all
 * cart-level or all item-level). `and`/`or` is only valid at the top level.
 * - Item-level strategies (`item_identifier`, `item_price`, `item_quantity`,
 * `item_category`, `item_attribute`, `item_custom_attribute`) — filter which
 * items the cart discount applies to.
 *
 * **Placement rules for `cart_total`:**
 * - Valid at the top level or as a direct child of `and`/`or` (all-cart siblings). - Not valid inside item-level condition children.
 */
export type Condition =
  | ({
      strategy?: "AndCondition"
    } & AndCondition)
  | ({
      strategy?: "OrCondition"
    } & OrCondition)
  | ({
      strategy?: "CartTotalCondition"
    } & CartTotalCondition)
  | ({
      strategy?: "ItemIdentifierCondition"
    } & ItemIdentifierCondition)
  | ({
      strategy?: "ItemPriceCondition"
    } & ItemPriceCondition)
  | ({
      strategy?: "ItemQuantityCondition"
    } & ItemQuantityCondition)
  | ({
      strategy?: "ItemCategoryCondition"
    } & ItemCategoryCondition)
  | ({
      strategy?: "ItemAttributeCondition"
    } & ItemAttributeCondition)
  | ({
      strategy?: "CartItemCustomAttributeCondition"
    } & CartItemCustomAttributeCondition)
  | ({
      strategy?: "ShippingTypeCondition"
    } & ShippingTypeCondition)
  | ({
      strategy?: "ItemsBundleCondition"
    } & ItemsBundleCondition)

/**
 * Logical AND condition requiring ALL child conditions to be satisfied. Only valid at the top level of an action condition.
 * Children must all be the same level — either all item-level strategies (e.g. `item_sku`, `item_price`) or all cart-level strategies (e.g. `cart_total`). Mixing cart-level and item-level children is not allowed.
 */
export type AndCondition = {
  /**
   * Logical AND strategy
   */
  strategy?: "and"
  children?: Array<BaseCondition>
}

/**
 * Logical OR condition requiring ANY child condition to be satisfied. Only valid at the top level of a `cart_discount` action condition.
 * The primary use case is combining cart-level strategies — for example, `or(cart_total gte 5000, cart_total lt 500)` to apply a discount when the cart total falls into one of multiple ranges.
 * Children must all be the same level (all cart-level or all item-level). Mixing cart-level and item-level children is not allowed.
 * For item-level targeting, prefer using `item_identifier` directly as the top-level condition rather than wrapping it in an `or`.
 */
export type OrCondition = {
  /**
   * Logical OR strategy
   */
  strategy?: "or"
  /**
   * Array of child conditions where ANY one can satisfy the requirement
   */
  children?: Array<BaseCondition>
}

/**
 * Whether items must match (in) or not match (nin) the specified identifiers
 */
export type Operator = "in" | "nin"

/**
 * Identifies specific items by SKU or Product ID for targeted discounting. This is the recommended approach for precise item targeting in action conditions.
 */
export type ItemIdentifierCondition = {
  /**
   * Strategy for matching items by SKU or Product ID
   */
  strategy?: "item_identifier"
  /**
   * Whether items must match (in) or not match (nin) the specified identifiers
   */
  operator?: "in" | "nin"
  /**
   * Object containing arrays of identifiers to match
   */
  args?: [
    {
      /**
       * SKUs to match against cart items
       */
      skus?: Array<string>
      /**
       * Product IDs to match against cart items
       */
      ids?: Array<string>
    },
  ]
  /**
   * Optional nested conditions to further refine item matching. Only `item_custom_attribute` strategies are allowed as children. When children are specified, items must match both the identifier (SKU/Product ID) AND all child conditions.
   */
  children?: Array<CartItemCustomAttributes>
}

/**
 * Price-based condition for targeting items based on their unit price. Allows filtering by price ranges using comparison operators.
 */
export type ItemPriceCondition = {
  /**
   * Strategy for matching items by price
   */
  strategy?: "item_price"
  /**
   * Comparison operator for price evaluation
   */
  operator?: "gt" | "gte" | "lt" | "lte" | "eq" | "ne"
  /**
   * Price value for comparison (in minor currency units)
   */
  args?: [number]
}

/**
 * Quantity-based condition for targeting items based on their cart quantity. Useful for bulk discount requirements or minimum quantity thresholds.
 */
export type ItemQuantityCondition = {
  /**
   * Strategy for matching items by quantity
   */
  strategy?: "item_quantity"
  /**
   * Comparison operator for quantity evaluation
   */
  operator?: "gt" | "gte" | "lt" | "lte" | "eq" | "ne"
  /**
   * Quantity value for comparison
   */
  args?: [number]
}

/**
 * Category-based condition for targeting items by their category breadcrumbs (i.e., the [hierarchies or nodes](/docs/api/pxm/products/hierarchies) associated with the item). Matches against any category in the item's hierarchical category path.
 */
export type ItemCategoryCondition = {
  /**
   * Strategy for matching items by category
   */
  strategy?: "item_category"
  /**
   * Whether items must be in (in) or not in (nin) the specified categories
   */
  operator?: "in" | "nin"
  /**
   * Category names to match against item breadcrumbs
   */
  args?: Array<string>
}

/**
 * Attribute-based condition for targeting items by their custom attributes. Allows complex filtering based on product metadata and characteristics.
 */
export type ItemAttributeCondition = {
  /**
   * Strategy for matching items by attributes
   */
  strategy?: "item_attribute"
  /**
   * Whether items must have (in) or not have (nin) the specified attribute values
   */
  operator?: "in" | "nin"
  args?: [
    string,
    string,
    "string" | "boolean" | "integer" | "float" | "date",
    number,
  ]
}

/**
 * Shipping type condition for targeting discounts based on shipping method. Only supported in shipping discount actions.
 */
export type ShippingTypeCondition = {
  /**
   * Strategy for matching shipping types
   */
  strategy?: "shipping_type"
  /**
   * Must match one of the specified shipping types
   */
  operator?: "in"
  /**
   * Shipping type identifiers to match against
   */
  args?: Array<string>
}

/**
 * Bundle condition for targeting specific item combinations. Only supported in items bundle discount actions for defining required item groups.
 */
export type ItemsBundleCondition = {
  /**
   * Strategy for matching item bundles
   */
  strategy?: "items_bundle"
  /**
   * Child conditions defining the required items in the bundle
   */
  children?: Array<BaseCondition>
}

/**
 * Specifies which items should be included or excluded when applying the discount. Can be based on factors such as item SKUs, product attributes, quantities, or categories.
 */
export type BaseCondition = {
  /**
   * The condition strategy type (e.g., item_identifier, item_category, or)
   */
  strategy?: string
  /**
   * The evaluation operator for the condition
   */
  operator?: string
  /**
   * Arguments/values for the condition evaluation
   */
  args?: Array<unknown>
  /**
   * Nested conditions for complex targeting requirements (level 1)
   */
  children?: Array<{
    strategy?: string
    operator?: string
    args?: Array<unknown>
    /**
     * Nested conditions (level 2)
     */
    children?: Array<{
      strategy?: string
      operator?: string
      args?: Array<unknown>
      /**
       * Nested conditions (level 3)
       */
      children?: Array<{
        strategy?: string
        operator?: string
        args?: Array<unknown>
      }>
    }>
  }>
}

export type And = {
  /**
   * Logical AND operator that requires ALL child conditions to be satisfied for the promotion to apply. Use when multiple criteria must be met simultaneously (e.g., minimum cart total AND specific customer segment).
   */
  strategy?: "and"
  /**
   * Array of child conditions that must ALL be satisfied.
   */
  children?: Array<{
    strategy?: string
    operator?: string
    args?: Array<unknown>
    children?: Array<unknown>
  }>
}

export type Or = {
  /**
   * Logical OR operator that requires ANY child condition to be satisfied for the promotion to apply. Provides flexible eligibility pathways, allowing multiple ways to qualify for a promotion.
   */
  strategy?: "or"
  children?: Array<{
    strategy?: string
    operator?: string
    args?: Array<unknown>
    children?: Array<unknown>
  }>
}

export type ItemSku = {
  /**
   * Considers products by matching against their SKU.  The `item_identifier` strategy is more flexible and recommended instead of this strategy.
   */
  strategy?: "item_sku"
  /**
   * Specifies the operators used for the rule strategy.
   */
  operator?: "in" | "nin"
  /**
   * The sku of the item to match, can be the sku of a [product](/docs/api/pxm/products/get-product) or a custom item.
   */
  args?: Array<string>
}

export type ItemIdentifier = {
  /**
   * Considers cart items matching by either SKU or Product ID. This strategy is preferred over `item_sku` and `item_product_id`, as it is more flexible.
   */
  strategy?: "item_identifier"
  /**
   * Specifies the operators used for the rule strategy.
   */
  operator?: "in" | "nin"
  args?: [
    {
      /**
       * SKU identifiers for specific product variants. Supports both catalog and custom items.
       */
      skus?: Array<string>
      /**
       * Product UUID identifiers for broader product targeting. Includes all variants of a product.
       */
      ids?: Array<string>
    },
  ]
  /**
   * Optional nested rules to further refine item matching. Only `item_custom_attribute` strategies are allowed as children. When children are specified, items must match both the identifier (SKU/Product ID) AND all child conditions.
   */
  children?: Array<CartItemCustomAttributes>
}

export type ItemProductId = {
  /**
   * Considers products by matching against their product ID. Can be used for items without a SKU like skuless bundles.
   * The `item_identifier` strategy is more flexible and recommended instead of this strategy.
   */
  strategy?: "item_product_id"
  /**
   * Specifies the operators used for the rule strategy.
   */
  operator?: "in" | "nin"
  /**
   * The `id` of the [products](/docs/api/pxm/products/get-product) to match.
   */
  args?: Array<string>
}

export type ItemCategory = {
  /**
   * Considers the item categories (i.e., the [hierarchy or node](/docs/api/pxm/products/hierarchies)) associated with the item. For particular items, the check is against the node of that item, and all parent nodes.
   */
  strategy?: "item_category"
  /**
   * Specifies the operators used for the rule strategy.
   */
  operator?: "in" | "nin"
  /**
   * An array of `id` values for [nodes or hierarchies](/docs/api/pxm/products/get-hierarchy-node)
   */
  args?: Array<string>
  children?: Array<{
    strategy?: string
    operator?: string
    args?: Array<unknown>
  }>
}

export type ItemAttribute = {
  /**
   * Considers the [Product Templates](/docs/api/pxm/products/extending-products-with-templates) associated with cart items.
   */
  strategy?: "item_attribute"
  /**
   * Specifies how the arguments are evaluated against the item.
   */
  operator?: "in" | "nin"
  args?: [
    string,
    string,
    "string" | "boolean" | "integer" | "float" | "date",
    number,
  ]
  children?: Array<{
    strategy?: string
    operator?: string
    args?: Array<unknown>
  }>
}

export type ItemPrice = {
  /**
   * Considers the price of items in the cart (after discount)
   */
  strategy?: "item_price"
  /**
   * Specifies the kind of restriction on the price.
   */
  operator?: "gte" | "gt" | "lte" | "lt" | "eq"
  /**
   * The which we evaluate the rule against.
   */
  args?: [number]
  children?: Array<{
    strategy?: string
    operator?: string
    args?: Array<unknown>
  }>
}

export type ItemQuantity = {
  /**
   * Considers the quantity of individual items.
   */
  strategy?: "item_quantity"
  /**
   * Specifies the kind of restriction on the quantity.
   */
  operator?: "gte" | "gt" | "lte" | "lt" | "eq"
  args?: [number]
}

export type ItemsBundle = {
  /**
   * Matches multiple sets of conditions against distinct cart items.
   */
  strategy?: "items_bundle"
  children?: Array<{
    strategy?: string
    operator?: string
    args?: Array<unknown>
    children?: Array<unknown>
  }>
}

export type CartTotal = {
  /**
   * Considers the total value of items in the shopping cart.
   */
  strategy?: "cart_total"
  /**
   * Specifies the kind of restriction on the total amount of the cart.
   */
  operator?: "gte" | "gt" | "lte" | "lt" | "eq" | "range"
  args?: CartTotalRangeArg | CartTotalArg
}

export type AccountTags = {
  /**
   * Considers the tags of the account associated with the cart.
   */
  strategy?: "account_tags"
  /**
   * Specifies how the arguments are evaluated against the account tags.
   */
  operator?:
    | "contains_all"
    | "contains_any"
    | "not_contains_any"
    | "not_contains_all"
  /**
   * The `id` of an account tag [Account Tag](/docs/api/accounts/list-account-tags)
   */
  args?: Array<string>
}

export type CartCustomAttributes = {
  /**
   * Considers custom attributes associated with the entire shopping cart.
   */
  strategy?: "cart_custom_attribute"
  /**
   * Specifies the restriction on cart custom_attributes for this rule promotion.
   * **Operator-Type Compatibility:** - `in` and `nin`: Support all types (string, boolean, integer, float). *Float matching is exact; floating-point precision can cause unexpected non-matches—use `gt`/`lt` for ranges when practical.* - `eq`: Supports string, boolean, and integer types with exactly 3 arguments - `gt` and `lt`: Support integer and float types with exactly 3 arguments - `gte` and `lte`: Support integer type only with exactly 3 arguments
   */
  operator?: "in" | "nin" | "eq" | "gt" | "lt" | "gte" | "lte"
  /**
   * Arguments for the cart custom attribute rule. The structure depends on the operator:
   * - For `in` and `nin`: [key, type, value1, value2, ...valueN] where N can be 1-20 values - For comparison operators (`eq`, `gt`, `lt`, `gte`, `lte`): [key, type, value] - exactly 3 arguments
   */
  args?: [string, "string" | "boolean" | "integer" | "float", number]
}

export type CartItemCustomAttributes = {
  /**
   * Considers custom attributes associated with individual cart items.
   */
  strategy?: "item_custom_attribute"
  /**
   * Specifies the restriction on cart item custom_attributes for this rule promotion.
   * **Operator-Type Compatibility:** - `in` and `nin`: Support all types (string, boolean, integer, float). *Float matching is exact; floating-point precision can cause unexpected non-matches—use `gt`/`lt` for ranges when practical.* - `eq`: Supports string, boolean, and integer types with exactly 3 arguments - `gt` and `lt`: Support integer and float types with exactly 3 arguments - `gte` and `lte`: Support integer type only with exactly 3 arguments
   */
  operator?: "in" | "nin" | "eq" | "gt" | "lt" | "gte" | "lte"
  /**
   * Arguments for the cart item custom attribute rule. The structure depends on the operator:
   * - For `in` and `nin`: [key, type, value1, value2, ...valueN] where N can be 1-20 values - For comparison operators (`eq`, `gt`, `lt`, `gte`, `lte`): [key, type, value] - exactly 3 arguments
   */
  args?: [string, "string" | "boolean" | "integer" | "float", number]
  /**
   * Optional nested rules that further refine the cart item custom attribute condition. Cart item custom attribute rules can have children to create more complex conditions.
   */
  children?: Array<{
    strategy?: string
    operator?: string
    args?: Array<unknown>
  }>
}

export type CartTotalRangeArg = [number]

export type CartTotalArg = [number, number]

/**
 * Apply a fixed discount to the amount.
 */
export type FixedArg = ["fixed", number]

/**
 * Apply a percentage based discount to the amount.
 */
export type PercentArg = ["percent", number]

/**
 * Cart total condition for gating a `cart_discount` action. The action is applied only when the cart total (or the total of a filtered subset of items) meets the threshold. When the condition passes, the discount applies to all cart items.
 * **Placement rules:**
 * - Valid as the top-level condition strategy. - Valid as a direct child of `and`/`or` (when all siblings are also cart-level). - **Not valid** inside item-level condition children.
 * **Children (optional):** item-level strategies that filter which items count toward the total check. For example, a `cart_total(gte 10000)` with an `item_identifier` child means "apply this action if the total of matching items is at least $100". The discount itself still applies to all cart items once the gate passes.
 */
export type CartTotalCondition = {
  /**
   * Cart total condition strategy
   */
  strategy: "cart_total"
  /**
   * Comparison operator for the cart total threshold.
   */
  operator: "gte" | "gt" | "lte" | "lt" | "eq" | "range"
  args: CartTotalArg | CartTotalRangeArg
  /**
   * Optional item-level conditions that filter which items count toward the total threshold check. Only leaf item strategies are allowed here — no `and`/`or` combinators and no nested `cart_total`.
   */
  children?: Array<BaseCondition>
}

/**
 * Custom attribute-based condition for targeting cart items by their custom attributes. Allows complex filtering based on item-level custom metadata.
 */
export type CartItemCustomAttributeCondition = {
  /**
   * Strategy for matching cart items by custom attributes
   */
  strategy?: "item_custom_attribute"
  /**
   * Operator for comparing custom attribute values
   */
  operator?: "in" | "nin" | "eq" | "gt" | "lt" | "gte" | "lte"
  /**
   * Arguments for the cart item custom attribute condition. The structure depends on the operator:
   * - For `in` and `nin`: [key, type, value1, value2, ...valueN] where N can be 1-20 values - For comparison operators (`eq`, `gt`, `lt`, `gte`, `lte`): [key, type, value] - exactly 3 arguments
   * **Float with `in`/`nin`:** Matching uses exact equality. Floating-point representation can cause values that look equal (e.g. after arithmetic) to not match; use `gt`/`lt` for numeric ranges when possible.
   */
  args?: [string, "string" | "boolean" | "integer" | "float", number]
}

/**
 * Set the amount to this fixed value.
 */
export type FixedPriceQuantityArg = ["fixed_price", number, number]

/**
 * Set the amount to this fixed value.
 */
export type FixedPriceArg = ["fixed_price", number]

export type PaginationLinks = {
  /**
   * URL for the current page of results
   */
  current?: string
  /**
   * URL for the first page of results
   */
  first?: string
  /**
   * URL for the last page of results
   */
  last?: string
  /**
   * URL for the next page of results
   */
  next?: string
  /**
   * URL for the previous page of results
   */
  prev?: string
}

export type PaginationMeta = {
  page?: {
    /**
     * Current page number
     */
    current?: number
    /**
     * Number of items per page
     */
    limit?: number
    /**
     * Offset from the first result
     */
    offset?: number
    /**
     * Total number of pages
     */
    total?: number
  }
  results?: {
    /**
     * Total number of results
     */
    total?: number
    /**
     * Method used to calculate the total (e.g., 'exact')
     */
    total_method?: string
  }
}

/**
 * The status of the job.
 */
export type Status =
  | "pending"
  | "processing"
  | "completed"
  | "failed"
  | "cancelling"
  | "cancelled"

export type PromotionJobResponse = PromotionJob & {
  /**
   * The status of the job.
   */
  status?:
    | "pending"
    | "processing"
    | "completed"
    | "failed"
    | "cancelling"
    | "cancelled"
  /**
   * The error encountered during job execution, if applicable.
   */
  error?: string
  meta?: {
    timestamps?: {
      /**
       * The creation date of the job.
       */
      created_at?: Date
      /**
       * The last updated date of the job.
       */
      updated_at?: Date
    }
  }
  result?: GenerateResult
}

/**
 * The Bearer token required to get access to the API.
 */
export type Authorization = string

/**
 * The maximum number of records per page for this response. You can set this value up to 100. If no page size is set, the [page length](/docs/commerce-cloud/global-project-settings/settings-overview#page-length) store setting is used.
 */
export type PageLimit = BigInt

/**
 * The current offset by number of records, not pages. Offset is zero-based. The maximum records you can offset is 10,000. If no page size is set, the [page length](/docs/commerce-cloud/global-project-settings/settings-overview#page-length) store setting is used.
 */
export type PageOffset = BigInt

export type GetRulePromotionsData = {
  body?: never
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path?: never
  query?: {
    /**
     * The maximum number of records per page for this response. You can set this value up to 100. If no page size is set, the [page length](/docs/commerce-cloud/global-project-settings/settings-overview#page-length) store setting is used.
     */
    "page[limit]"?: BigInt
    /**
     * The current offset by number of records, not pages. Offset is zero-based. The maximum records you can offset is 10,000. If no page size is set, the [page length](/docs/commerce-cloud/global-project-settings/settings-overview#page-length) store setting is used.
     */
    "page[offset]"?: BigInt
    /**
     * This parameter accepts a filtering expression that uses specific operators and attributes.
     *
     *
     * The following operators and attributes are available when filtering on this endpoint. See [Supported Filtering Characters](/guides/Getting-Started/filtering#supported-characters).
     *
     * | Attribute                 | Type               | Operator                     | Example                                                               |
     * |:--------------------------|:-------------------|:-----------------------------|:----------------------------------------------------------------------|
     * | `code`                    | `string`, `number` | `eq`                         | `eq(code,summer2024)`                                                 |
     * | `name`                    | `string`           | `like`,`ilike`               | `ilike(name,'Summer *')`                                              |
     * | `enabled`                 | `boolean`          | `eq`                         | `eq(enabled, true)`                                                   |
     * | `stackable`               | `boolean`          | `eq`                         | `eq(stackable, true)`                                                 |
     * | `override_stacking`       | `boolean`          | `eq`                         | `eq(override_stacking, true)`                                         |
     * | `start`                   | `date`             | `lt`, `le`, `eq`, `gt`, `ge` | `gt(start, 2025-01-01T00:00:00.000Z)`                                 |
     * | `end`                     | `date`             | `lt`, `le`, `eq`, `gt`, `ge` | `lt(end, 2025-01-01T00:00:00.000Z)`                                   |
     * | `rule_set.rules.strategy` | `string`           | `eq`, `in`                   | `eq(rule_set.rules.strategy,account_tags)`                            |
     * | `rule_set.rules.args`     | `string`           | `contains`                   | `contains(rule_set.rules.args, 11111111-1111-4111-1111-111111111111)` |
     * | `account_tags`            | `string`           | `contains`                   | `contains(account_tags, 11111111-1111-4111-1111-111111111111)`              |
     *
     * Some notes about filters:
     * 1. The `code` filter is case-insensitive. You can search for codes using only `numbers` or codes containing both `string` and `numbers`.
     * 2. The `rule_set.rules.*` filters do not search child conditions at present
     * 3. The `account_tags` filter will essentially search for any rule or child rule that has an `account_tags` strategy and the associated accounts.
     *
     */
    filter?: string
  }
  url: "/v2/rule-promotions"
}

export type GetRulePromotionsErrors = {
  /**
   * Bad Request
   */
  400: ResponseError
}

export type GetRulePromotionsError =
  GetRulePromotionsErrors[keyof GetRulePromotionsErrors]

export type GetRulePromotionsResponses = {
  /**
   * OK
   */
  200: RulePromotionListResponse
}

export type GetRulePromotionsResponse =
  GetRulePromotionsResponses[keyof GetRulePromotionsResponses]

export type CreateRulePromotionData = {
  body: RulePromotionItem
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path?: never
  query?: never
  url: "/v2/rule-promotions"
}

export type CreateRulePromotionErrors = {
  /**
   * Bad Request
   */
  400: ResponseError
  /**
   * Duplicate Priority
   */
  422: ResponseError
}

export type CreateRulePromotionError =
  CreateRulePromotionErrors[keyof CreateRulePromotionErrors]

export type CreateRulePromotionResponses = {
  /**
   * Created
   */
  201: RulePromotionResponse
}

export type CreateRulePromotionResponse =
  CreateRulePromotionResponses[keyof CreateRulePromotionResponses]

export type DeleteRulePromotionData = {
  body?: never
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the promotion.
     */
    promotionID: string
  }
  query?: never
  url: "/v2/rule-promotions/{promotionID}"
}

export type DeleteRulePromotionResponses = {
  /**
   * No Content
   */
  204: void
}

export type DeleteRulePromotionResponse =
  DeleteRulePromotionResponses[keyof DeleteRulePromotionResponses]

export type GetRulePromotionByIdData = {
  body?: never
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    promotionID: string
  }
  query?: never
  url: "/v2/rule-promotions/{promotionID}"
}

export type GetRulePromotionByIdErrors = {
  /**
   * Not Found
   */
  404: ResponseError
}

export type GetRulePromotionByIdError =
  GetRulePromotionByIdErrors[keyof GetRulePromotionByIdErrors]

export type GetRulePromotionByIdResponses = {
  /**
   * OK
   */
  200: RulePromotionResponse
}

export type GetRulePromotionByIdResponse =
  GetRulePromotionByIdResponses[keyof GetRulePromotionByIdResponses]

export type UpdateRulePromotionData = {
  body: RulePromotionItem
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the promotion to be updated.
     */
    promotionID: string
  }
  query?: never
  url: "/v2/rule-promotions/{promotionID}"
}

export type UpdateRulePromotionErrors = {
  /**
   * Bad Request
   */
  400: ResponseError
  /**
   * Unprocessable Entity
   */
  422: ResponseError
}

export type UpdateRulePromotionError =
  UpdateRulePromotionErrors[keyof UpdateRulePromotionErrors]

export type UpdateRulePromotionResponses = {
  /**
   * OK
   */
  200: RulePromotionResponse
}

export type UpdateRulePromotionResponse =
  UpdateRulePromotionResponses[keyof UpdateRulePromotionResponses]

export type DeleteRulePromotionCodesData = {
  body: PromotionCodesRequest
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    promotionID: string
  }
  query?: never
  url: "/v2/rule-promotions/{promotionID}/codes"
}

export type DeleteRulePromotionCodesErrors = {
  /**
   * Bad Request
   */
  400: ResponseError
}

export type DeleteRulePromotionCodesError =
  DeleteRulePromotionCodesErrors[keyof DeleteRulePromotionCodesErrors]

export type DeleteRulePromotionCodesResponses = {
  /**
   * No Content
   */
  204: void
}

export type DeleteRulePromotionCodesResponse =
  DeleteRulePromotionCodesResponses[keyof DeleteRulePromotionCodesResponses]

/**
 * Specifies the order of the codes that will be returned.
 *
 */
export type Sort = "code" | "-code"

export type GetRulePromotionCodesData = {
  body?: never
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    promotionID: string
  }
  query?: {
    /**
     * This parameter accepts a filtering expression that uses specific operators and attributes. Promotion codes are case-insensitive.
     *
     * The following operators and attributes are available when filtering on this endpoint. See [Supported Filtering Characters](/guides/Getting-Started/filtering#supported-characters).
     *
     * | Attribute | Type     | Operator    | Example        |
     * |:--------- |:---------|:------------|:---------------|
     * | `code`    | `string`, `number` |  `eq`, `gt`       |  `eq(code,summer2024)`, `gt(code,2024)` |
     *
     */
    filter?: string
    /**
     * Specifies the order of the codes that will be returned.
     *
     */
    sort?: "code" | "-code"
  }
  url: "/v2/rule-promotions/{promotionID}/codes"
}

export type GetRulePromotionCodesResponses = {
  /**
   * OK
   */
  200: GetPromotionCodesResponse
}

export type GetRulePromotionCodesResponse =
  GetRulePromotionCodesResponses[keyof GetRulePromotionCodesResponses]

export type CreateRulePromotionCodesData = {
  body: PromotionCodesRequest
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    promotionID: string
  }
  query?: never
  url: "/v2/rule-promotions/{promotionID}/codes"
}

export type CreateRulePromotionCodesErrors = {
  /**
   * Bad Request
   */
  400: {
    errors?: Array<{
      status?: number
      source?: string
      title?: string
      detail?: string
    }>
  }
  /**
   * Unprocessable Entity
   */
  422: {
    errors?: Array<{
      status?: string
      source?: string
      title?: string
      detail?: string
    }>
  }
}

export type CreateRulePromotionCodesError =
  CreateRulePromotionCodesErrors[keyof CreateRulePromotionCodesErrors]

export type CreateRulePromotionCodesResponses = {
  /**
   * Created
   */
  201: CreatePromotionCodesResponse
}

export type CreateRulePromotionCodesResponse =
  CreateRulePromotionCodesResponses[keyof CreateRulePromotionCodesResponses]

export type DeleteSingleRulePromotionCodeData = {
  body?: never
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    promotionID: string
    /**
     * The unique identifier of the rule promotion code.
     */
    codeID: string
  }
  query?: never
  url: "/v2/rule-promotions/{promotionID}/codes/{codeID}"
}

export type DeleteSingleRulePromotionCodeResponses = {
  /**
   * No Content
   */
  204: void
}

export type DeleteSingleRulePromotionCodeResponse =
  DeleteSingleRulePromotionCodeResponses[keyof DeleteSingleRulePromotionCodeResponses]

export type GetRulePromotionJobsData = {
  body?: never
  path: {
    /**
     * The unique identifier of a rule promotion.
     */
    uuid: string
  }
  query?: {
    /**
     * Specifies filter attributes.
     */
    filter?: string
  }
  url: "/v2/rule-promotions/{uuid}/jobs"
}

export type GetRulePromotionJobsResponses = {
  /**
   * Successful response
   */
  200: PromotionJobsListResponse
}

export type GetRulePromotionJobsResponse =
  GetRulePromotionJobsResponses[keyof GetRulePromotionJobsResponses]

export type CreateRulePromotionJobData = {
  body: {
    data?: PromotionJob
  }
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    uuid: string
  }
  query?: never
  url: "/v2/rule-promotions/{uuid}/jobs"
}

export type CreateRulePromotionJobErrors = {
  /**
   * Bad Request
   */
  400: {
    errors?: Array<{
      status?: string
      title?: string
      detail?: string
    }>
  }
}

export type CreateRulePromotionJobError =
  CreateRulePromotionJobErrors[keyof CreateRulePromotionJobErrors]

export type CreateRulePromotionJobResponses = {
  /**
   * Promotion job response
   */
  201: {
    data?: PromotionJobResponse
  }
}

export type CreateRulePromotionJobResponse =
  CreateRulePromotionJobResponses[keyof CreateRulePromotionJobResponses]

export type GetRulePromotionJobFileData = {
  body?: never
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    uuid: string
    /**
     * The unique identifier of the job associated with the file.
     */
    "job-uuid": string
  }
  query?: never
  url: "/v2/rule-promotions/{uuid}/jobs/{job-uuid}/file"
}

export type GetRulePromotionJobFileResponses = {
  /**
   * Successful response
   */
  200: PromotionCodeExportedFileResponse
}

export type GetRulePromotionJobFileResponse =
  GetRulePromotionJobFileResponses[keyof GetRulePromotionJobFileResponses]

export type CancelRulePromotionJobData = {
  body?: never
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    uuid: string
    /**
     * The unique identifier of the job to be canceled.
     */
    "job-uuid": string
  }
  query?: never
  url: "/v2/rule-promotions/{uuid}/jobs/{job-uuid}/cancel"
}

export type CancelRulePromotionJobErrors = {
  /**
   * Unprocessable Entity
   */
  422: {
    errors?: Array<{
      status?: string
      title?: string
      detail?: string
    }>
  }
}

export type CancelRulePromotionJobError =
  CancelRulePromotionJobErrors[keyof CancelRulePromotionJobErrors]

export type CancelRulePromotionJobResponses = {
  /**
   * Promotion job response
   */
  200: {
    data?: PromotionJobResponse
  }
}

export type CancelRulePromotionJobResponse =
  CancelRulePromotionJobResponses[keyof CancelRulePromotionJobResponses]

export type AnonymizeRulePromotionUsagesData = {
  body: {
    data: {
      /**
       * The unique identifiers of the usages to be anonymized.
       * Multiple usage IDs can be provided to anonymize in bulk.
       *
       */
      usage_ids?: Array<string>
    }
  }
  path?: never
  query?: never
  url: "/v2/rule-promotions/usages/anonymize"
}

export type AnonymizeRulePromotionUsagesErrors = {
  /**
   * Bad Request
   */
  400: ResponseError
  /**
   * Unauthorized
   */
  401: ResponseError
  /**
   * Not Found
   */
  404: ResponseError
}

export type AnonymizeRulePromotionUsagesError =
  AnonymizeRulePromotionUsagesErrors[keyof AnonymizeRulePromotionUsagesErrors]

export type AnonymizeRulePromotionUsagesResponses = {
  /**
   * OK
   */
  200: {
    data?: Array<RulePromotionUsage>
  } & ResponseError
}

export type AnonymizeRulePromotionUsagesResponse =
  AnonymizeRulePromotionUsagesResponses[keyof AnonymizeRulePromotionUsagesResponses]

export type GetRulePromotionUsagesData = {
  body?: never
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    promotionID: string
  }
  query?: {
    /**
     * Filter attributes to refine the usage records. Supported attributes:
     * - `id`: Filter by usage ID.
     * - `code`: Filter by promotion code.
     * - `used_on`: Filter by usage date with operators `gt`, `ge`, `le`, `lt`.
     *
     */
    filter?: string
    /**
     * The number of records per page.
     */
    "page[limit]"?: number
    /**
     * The number of records to offset the results by.
     */
    "page[offset]"?: number
  }
  url: "/v2/rule-promotions/{promotionID}/usages"
}

export type GetRulePromotionUsagesErrors = {
  /**
   * Unauthorized
   */
  401: ResponseError
  /**
   * Not Found
   */
  404: ResponseError
}

export type GetRulePromotionUsagesError =
  GetRulePromotionUsagesErrors[keyof GetRulePromotionUsagesErrors]

export type GetRulePromotionUsagesResponses = {
  /**
   * Successful response
   */
  200: {
    data?: Array<RulePromotionUsage>
    links?: PaginationLinks
    meta?: PaginationMeta
  }
}

export type GetRulePromotionUsagesResponse =
  GetRulePromotionUsagesResponses[keyof GetRulePromotionUsagesResponses]

export type GetRulePromotionCodeUsagesData = {
  body?: never
  headers: {
    /**
     * The Bearer token required to get access to the API.
     */
    Authorization: string
  }
  path: {
    /**
     * The unique identifier of the rule promotion.
     */
    promotionID: string
    /**
     * The specific promotion code for which to retrieve usage records.
     */
    code: string
  }
  query?: {
    /**
     * Filter attributes to refine the results. Supported attributes:
     * - `id`: Filter by usage ID.
     * - `used_on`: Filter by date with operators `gt`, `ge`, `le`, `lt`.
     *
     */
    filter?: string
    /**
     * The number of records per page.
     */
    "page[limit]"?: number
    /**
     * The number of records to offset the results by.
     */
    "page[offset]"?: number
  }
  url: "/v2/rule-promotions/{promotionID}/codes/{code}/usages"
}

export type GetRulePromotionCodeUsagesErrors = {
  /**
   * Unauthorized
   */
  401: ResponseError
  /**
   * Not Found
   */
  404: ResponseError
}

export type GetRulePromotionCodeUsagesError =
  GetRulePromotionCodeUsagesErrors[keyof GetRulePromotionCodeUsagesErrors]

export type GetRulePromotionCodeUsagesResponses = {
  /**
   * Successful response
   */
  200: {
    data?: Array<RulePromotionUsage>
    links?: PaginationLinks
    meta?: PaginationMeta
  }
}

export type GetRulePromotionCodeUsagesResponse =
  GetRulePromotionCodeUsagesResponses[keyof GetRulePromotionCodeUsagesResponses]
